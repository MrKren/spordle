import React, { VFC } from "react";
import { Box } from "@mui/material";
import { AuthPanelProps } from "../types";

const AuthPanel: VFC<AuthPanelProps> = ({setAuthenticated}) => {
  return <Box></Box>;
};

export default AuthPanel;
